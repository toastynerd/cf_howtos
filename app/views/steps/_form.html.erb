<%= form_for [@howto, @step] do |f| %>
  <%= f.error_messages %>
  <div class="panel">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="panel">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>
  <%= f.submit :class => "button" %>
<% end %>